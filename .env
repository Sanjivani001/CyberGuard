PORT=5000
MONGO_URI=mongodb+srv://sanju010305_db_user:aVF5vn7AkoMDaavE@cluster0.o34xhas.mongodb.net/CyberGuard
IPQS_KEY=bwwE3a6ItMRdcsykLWtm8ulgg2d8fo82
# Comma-separated list of URLs that backend will auto-scan periodically:
# DISABLED for testing (IPQS free tier quota exhausted) — enable tomorrow or after upgrading
MONITOR_URLS=
# Risk threshold (0-100) to consider suspicious and save alert
RISK_THRESHOLD=50
# Auto-scan interval in milliseconds (e.g., 5min = 300000) — set to 1 hour when re-enabled
SCAN_INTERVAL_MS=3600000
# Optional: set BACKEND_PUBLIC_URL if deploying (used by extension popup)
BACKEND_PUBLIC_URL=http://localhost:5000
# Allow server to start even if MongoDB is unreachable (dev only). Set to 'true' to enable.
ALLOW_OFFLINE_START=true
